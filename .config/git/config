# vim:ft=gitconfig:tabstop=2

[user]
	name = Ta-Seen
	email = taseen00.islam@gmail.com
	signingkey = 4B5ECC34EDAF44B3

[core]
	editor = nvim
	pager = delta

[commit]
	gpgsign = true

[delta]
	navigate = true
	syntax-theme = Kanagawa

[diff]
	colorMoved = default
	submodule = log

[init]
	defaultBranch = main

[interactive]
	diffFilter = delta --color-only

[merge]
	conflictStyle = diff3

[pull]
	ff = only

# The repeated `helper =` lines are intentional:
# https://github.com/cli/cli/issues/3796#issuecomment-1065150465
[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential

# UTS GitLab repositories
[includeIf "hasconfig:remote.*.url:https://code.research.uts.edu.au/**"]
	path = ./config-uts
